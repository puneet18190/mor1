<%= render 'search_header', options: @options, cardgroups: @cardgroups, show_search: @show_search %>

<% if @total_calls.to_i > 0 %>
  <%= link_to b_csv + _('Export_to_CSV'), { action: :aggregate, csv: 1 }, { style: 'float:right' } %>
<% end %>
<br/><br/>

<%= page_select_header(@options[:page], @total_pages, nil, @options) %>

<%= render 'aggregate_call_list', options: @options, result: @result, total: @total %>
<br/>

<%= page_select_header(@options[:page], @total_pages, nil, @options) %>
