<div align="center">
  <table class='page_title2' width="100%">
    <tr>
      <td align="center">
        <% for letter in 'A'..'Z' %>
            <%= raw '<b>' if letter == st %>
            <%= raw link_to letter, action: params[:action], id: letter_select_header_id, st: letter %>
            <%= raw '</b>' if letter == st %>
        <% end %>
      </td>
    </tr>
  </table>
</div>
